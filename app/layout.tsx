import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MatchmakIEEEng | IEEE Student Branch IPB",
  description: "Team matchmaking application for IEEE Student Branch IPB University members.",
  icons: {
    icon: "/images/layout/Logo_IEEE_IPB_Icon.png",
    shortcut: "/images/layout/Logo_IEEE_IPB_Icon.png",
    apple: "/images/layout/Logo_IEEE_IPB_Icon.png",
  },
};

export default function RootLayout({ children }: LayoutProps<"/">) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
